<!DOCTYPE html>
html(lang='en')
    head
       meta(charset="UTF-8")
       meta(name="viewport" content="width=device-width, initial-scale=1.0")
       title #{title}
       link(rel="stylesheet" href="/css/main.css")
       block styles
    body
     header
      ul
       li
        a(href="/" ,class = (path === '/' ? 'active' : ' ')) Posts
       li
        a(href="/admin/add-posts",class = (path === 'admin/add-posts' ? 'active' : ' ')) Add Post
     main 
        block content
       


     